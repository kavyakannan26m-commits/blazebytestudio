'use client'; import {useState} from 'react'; import Link from 'next/link'; import {useRouter} from 'next/navigation'; import {createSupabaseBrowserClient} from '@/lib/supabase/client';
export default function Register(){const r=useRouter(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');try{const s=createSupabaseBrowserClient();const {data,error}=await s.auth.signUp({email,password,options:{data:{full_name:name}}});if(error)throw error;if(data.session)r.push('/dashboard');else setError('Check your email to confirm your account, then log in.')}catch(e){setError(e instanceof Error?e.message:'Unable to register.')}}return <div className="grid min-h-screen place-items-center px-4"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-white/[.03] p-8"><p className="eyebrow">BlazeByte Studio</p><h1 className="mt-3 text-3xl font-bold">Create your account</h1><form onSubmit={submit} className="mt-8 space-y-4"><label>Name<input value={name} onChange={e=>setName(e.target.value)} required className="field"/></label><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="field"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required className="field"/></label>{error&&<p className="text-sm text-amber-200">{error}</p>}<button className="btn">Create account</button><p className="text-sm text-slate-400">Already registered? <Link className="text-cyan-300" href="/login">Log in</Link></p></form></div></div>}
